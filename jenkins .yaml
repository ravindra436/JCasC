credentials:
  system:
    domainCredentials:
        credentials:
          - usernamePassword:
              scope: SYSTEM
              id: sudo_password
              username: root
              password: ravindra
